# 仿真进度条优化说明

## 概述
为了方便查看仿真进度，已在关键耗时操作中添加了进度条显示。使用 `tqdm` 库实现。

## 修改的文件

### 1. Calself/constellation.py
添加了 `from tqdm import tqdm` 导入

**修改的函数：**

#### update_constellations()
- **功能**：更新所有卫星的轨道位置
- **进度条**：显示卫星位置更新进度
- **输出示例**：
  ```
  更新卫星位置: 5000it [00:45, 110.89it/s]
  update constellation position and cellular success (耗时: 45.2345秒, 共5000颗卫星)
  ```

#### initial_constellations()
- **功能**：初始化地面站可见卫星集合
- **进度条**：显示地面站初始化进度
- **输出示例**：
  ```
  初始化地面站可见卫星集合: 10000it [00:30, 333.33it/s]
  ```

#### update_constellation()
- **功能**：更新地面站卫星切换和干扰链接
- **进度条**：
  - 更新地面站卫星切换进度
  - 清理干扰链接进度
  - 更新干扰链接进度
- **输出示例**：
  ```
  更新地面站卫星切换: 10000it [00:25, 400.00it/s]
  清理干扰链接: 10000it [00:10, 1000.00it/s]
  更新干扰链接: 10000it [00:15, 666.67it/s]
  ```

### 2. Calself/earth_station.py
添加了 `from tqdm import tqdm` 导入

**修改的函数：**

#### update_earth_stations()
- **功能**：更新所有地面站的位置坐标
- **进度条**：显示地面站位置更新进度
- **输出示例**：
  ```
  更新地面站位置: 10000it [00:20, 500.00it/s]
  Earth stations position update success (耗时: 20.1234秒, 共10000个地面站)
  ```

### 3. Calself/inference_link.py
添加了 `from tqdm import tqdm` 导入

**修改的函数：**

#### get_inference_constellations()
- **功能**：计算所有地面站的干扰链接
- **进度条**：显示干扰链接计算进度
- **输出示例**：
  ```
  计算干扰链接: 10000it [02:30, 66.67it/s]
  ```

#### get_total_constellation_inference()
- **功能**：计算所有星座之间的总干扰
- **进度条**：
  - 汇总干扰统计进度
  - 验证干扰链接一致性进度
- **输出示例**：
  ```
  汇总干扰统计: 50000it [00:45, 1111.11it/s]
  验证干扰链接一致性: 10000it [00:30, 333.33it/s]
  ```

## 进度条特性

### 显示信息
每个进度条显示：
- 当前进度（已完成/总数）
- 耗时
- 处理速度（items/s）
- 预计剩余时间

### 示例输出
```
更新卫星位置: 45%|████▌     | 2250/5000 [00:20<00:25, 110.89it/s]
```

## 性能影响
- `tqdm` 库的开销极小（< 1%）
- 进度条显示不会显著影响仿真性能
- 可以通过禁用进度条来完全消除开销（如需要）

## 使用建议

### 查看完整进度
运行仿真时，您现在可以看到：
1. 卫星位置更新进度
2. 地面站位置更新进度
3. 干扰链接计算进度
4. 总干扰统计进度

### 性能分析
通过进度条的速度信息，可以识别：
- 哪个步骤最耗时
- 处理速度是否符合预期
- 是否存在性能瓶颈

## 依赖要求
```bash
pip install tqdm
```

## 示例运行输出
```
set system parameter success
2024-12-16 00:00:02
更新卫星位置: 100%|██████████| 5000/5000 [00:45<00:00, 110.89it/s]
update constellation position and cellular success (耗时: 45.2345秒, 共5000颗卫星)
更新地面站位置: 100%|██████████| 10000/10000 [00:20<00:00, 500.00it/s]
Earth stations position update success (耗时: 20.1234秒, 共10000个地面站)
初始化地面站可见卫星集合: 100%|██████████| 10000/10000 [00:30<00:00, 333.33it/s]
计算干扰链接: 100%|██████████| 10000/10000 [02:30<00:00, 66.67it/s]
汇总干扰统计: 100%|██████████| 50000/50000 [00:45<00:00, 1111.11it/s]
```

## 注意事项
- 进度条在终端中显示最佳效果
- 如果输出被重定向到文件，进度条会自动禁用
- 所有原有功能保持不变，仅添加了进度显示
