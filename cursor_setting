ALWAYS REPLY IN CHINESE.

When generating or modifying code in this workspace, follow these rules:

1. 语言与注释
- 所有解释性文字一律使用中文回答。
- 所有对外可见的“计划/思考过程/分析步骤/进度更新”一律使用中文表述（不要求也不输出不可公开的内部推理细节）。
- 生成的代码（尤其是 Python）中，关键逻辑、易错点、边界条件需要添加中文注释。
- 为函数、类编写中文 docstring，遵循示例格式：

  def function_name(param1, param2):
      """
      函数功能简述。

      Args:
          param1: 参数1说明
          param2: 参数2说明

      Returns:
          返回值说明
      """

2. Python 代码风格
- 严格遵守 PEP8。
- 使用 4 空格缩进。
- 变量和函数名使用 snake_case。
- 类名使用 PascalCase。
- 常量使用 UPPER_SNAKE_CASE。
- 保持实现简单直接，一个函数只做一件事，尽量避免超过 3 层的深层嵌套。

3. 工作习惯
- 修改代码前，先简要分析并说明对现有代码的理解，避免无关重构。
- 遵循“最小改动原则”：只修改与当前需求直接相关的部分。
- 对较大范围修改，先给出简要方案，再给出分步骤的代码修改建议。
- 代码修改完成后，用简洁的中文总结改动点，而不是生成冗长的报告文档。

4. 输出风格
- 使用 Markdown，小标题使用“## / ###”，关键点用加粗。
- 回答保持信息密度高、结构清晰，避免无意义的客套话。
- 如果用户只需要代码，先给出精简代码片段，并在必要时用简短中文说明关键点。